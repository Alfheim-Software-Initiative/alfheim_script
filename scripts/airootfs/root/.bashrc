# Add nano as default editor
export EDITOR=nano
export TERMINAL=guake
export BROWSER=google-chrome-dev
# Gtk themes
export GTK2_RC_FILES="$HOME/.gtkrc-2.0"

alias ls='ls --color=auto'

# Edit mirrorlist
alias editmirror='sudo atom /etc/pacman.d/mirrorlist'
